// action creators
// returns an action (object)

// If I need to make a change, and it will be reflected everywhere

const BUY_BALL = 'BUY_BALL';

const BallActions = () => {
    return { type: BUY_BALL };
}